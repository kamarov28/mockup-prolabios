APP_NAME="Prolabios"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://prolabios.com

# ⚠️ CRITICAL: APP_DEBUG must be false in production!
# When true, Laravel exposes stack traces, file paths, and environment variables.
# This is a severe security risk. Only enable for local development.

ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_password_to_a_secure_value

APP_LOCALE=id
APP_FALLBACK_LOCALE=id
APP_FAKER_LOCALE=id_ID

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# Analytics & Tracking (P0 - High Impact)
GOOGLE_ANALYTICS_ID=
GOOGLE_TAG_MANAGER_ID=

# Bot Protection & Security
# Isi salah satu (reCAPTCHA v3 ATAU Cloudflare Turnstile) saat deploy production.
# Jika keduanya kosong: honeypot + rate limiter saja (OK untuk local).
# Jika diisi: token wajib; gagal API di production = form ditolak (fail closed).
RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
# production: warning|error — local dev may use debug
LOG_LEVEL=warning

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# Cache: use redis in production (database driver adds a DB round-trip per cache hit).
# Local without Redis: CACHE_STORE=database or file is fine.
CACHE_STORE=redis
# CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=smtp.mailgun.org
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="marketing@prolabios.com"
MAIL_FROM_NAME="PT. Prolabios Mitra Analitika"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
